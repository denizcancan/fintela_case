# Local Database Configuration (for Docker Compose)
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_USER=denizcan
POSTGRES_PASSWORD=fintela_password
POSTGRES_DB=fintela

# Full connection string (optional - will be constructed from above if not set)
# DATABASE_URL=postgresql://fintela:fintela_password@localhost:5432/fintela

# Local Service Ports
FASTAPI_PORT=8000
DAGSTER_PORT=3000
DASHBOARD_PORT=5173
